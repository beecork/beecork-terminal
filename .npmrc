# Turn package.json's `engines` from advisory into enforced: npm otherwise just
# prints EBADENGINE and installs anyway. The failure this prevents is the v0.1.25
# CI break — jsdom's bundled undici needs `webidl.util.markAsUncloneable`, absent
# in Node 20, and the resulting error names nothing about the Node version.
engine-strict=true
